<Window x:Class="AlarmAndTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlarmAndTimer"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="350"
        WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        
        Background="White"
        BorderBrush="#CCCCCC"
        BorderThickness="3">
    <Window.ContextMenu>
        <ContextMenu Opened="ContextMenu_Opened">
            <MenuItem Name="MakeTimerContextMenu" Header="타이머 생성" Click="OpenTimerPanel_Click" />
            <MenuItem Name="MakeAlarmContextMenu" Header="알람 생성" Click="OpenAlarmPanel_Click" />
            <Separator />
            <MenuItem Header="설정" />
            <MenuItem Header="로드" />
            <Separator />
            <MenuItem Header="항상 위" 
                  Name="AlwaysTopContextMenu"
                  IsCheckable="True"
                  Checked="AlwaysTop_Checked" 
                  Unchecked="AlwaysTop_Unchecked" />
            <MenuItem Header="트레이로 보내기" Click="TrayMenu_HyperLink" />
            <MenuItem Header="최소화" Click="Minimize_HyperLink" />
            <MenuItem Header="완전히 종료" Click="CompletelyClose_HyperLink" />
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Collapsed">

                <CheckBox Name="AlwaysTop_CheckBox"
                          VerticalAlignment="Center"
                          IsChecked="{Binding IsAlwaysTop, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Checked="AlwaysTop_Checked"
                          Unchecked="AlwaysTop_Unchecked"
                          Margin="0,0,-7,0"/>
                <TextBlock Margin="10,0,10,0"
                           VerticalAlignment="Center">
                    <Hyperlink Click="AlwaysTop_HyperLink">항상위 고정</Hyperlink>
                </TextBlock>
                <TextBlock Margin="10,0,10,0"
                           VerticalAlignment="Center">
                    <Hyperlink Click="TrayMenu_HyperLink">트레이 보내기</Hyperlink>
                </TextBlock>
                <TextBlock Margin="10,0,10,0"
                           VerticalAlignment="Center">
                    <Hyperlink Click="Minimize_HyperLink">최소화</Hyperlink>
                </TextBlock>
                <TextBlock Margin="10,0,10,0"
                           VerticalAlignment="Center">
                    <Hyperlink Click="CompletelyClose_HyperLink">완전히 종료</Hyperlink>
                </TextBlock>
            </StackPanel>
            <ListView
                    Grid.Row="1"
                    x:Name="TimerListView"
                    BorderBrush="Transparent"
                    Background="Transparent"
                    HorizontalContentAlignment="Stretch"
                    Margin="0,5,0,0"
    
                    ItemsSource="{Binding Timers}"
                    d:DataContext="{d:DesignInstance Type=local:DesignViewModel, IsDesignTimeCreatable=True}">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                    Text="{Binding TimeLeftString}" 
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Margin="0,0,15,0"
                                    VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="1"
                                    Text="{Binding EndTimeString, StringFormat='종료: {0}'}" 
                                    FontSize="12"
                                    Foreground="Gray"
                                    VerticalAlignment="Center"/>

                            <TextBlock Grid.Column="2"
                                    Text="{Binding TimerMemo}"/>

                            <Button Grid.Column="3"
                                    Click="PauseTimer_Click"
                                    Content="{Binding PauseButtonText}"
                                    Padding="5,2"
                                    FontSize="11"
                                    Margin="0,0,5,0"
                                    Background="#FFF0F0"
                                    BorderBrush="#FFE0E0"
                                    VerticalAlignment="Center"/>

                            <Button Grid.Column="4"
                                    Click="DeleteTimer_Click"
                                    Content="삭제"
                                    Padding="5,2"
                                    FontSize="11"
                                    Background="#FFF0F0"
                                    BorderBrush="#FFE0E0"
                                    VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Name="AddButtonPackage" VerticalAlignment="Bottom" HorizontalAlignment="Right" Visibility="Hidden">

            <StackPanel x:Name="SubButtonsPanel" Visibility="Collapsed" Margin="0,0,0,10">
                <Button Content="설정" Height="30" Margin="0,5"/>
                <Button Content="로드" Height="30" Margin="0,5"/>
                <Button Content="알람" Height="30" Margin="0,5" Click="OpenAlarmPanel_Click"/>
                <Button Content="타이머" Height="30" Margin="0,5" Click="OpenTimerPanel_Click"/>
            </StackPanel>

            <Button Content="+" Width="50" Height="50" Click="AddButton_Click"/>
        </StackPanel>
        <Grid Name="MakePanelBackground"
              Visibility="Hidden"
              MouseLeftButtonDown="CloseMakePanel">
            <Border Background="#1A000000" />
        </Grid>
        <StackPanel Name="AlarmMakePanel" VerticalAlignment="Bottom" Visibility="Hidden">
            <Border Background="Red" CornerRadius="10, 10, 0, 0">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0, 30, 0, 30">
                    <StackPanel>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <Button
                                    Name="AmPmButton"
                                     Content=""
                                     Width="30"
                                     Click="ToggleAmPm_Click"
                                     Margin="0,0,4,0" />
                                <TextBox Grid.Column="0" 
                                    Name="AlarmHourInput"
                                    Width="30"
                                    VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="1" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmHourUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmHourDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="2" Text="시" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                                <TextBox Grid.Column="3" 
                            Name="AlarmMinuteInput"
                            Width="30" 
                            VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="4" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmMinuteUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmMinuteDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="5" Text="분" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                                <TextBox Grid.Column="6" 
                            Name="AlarmSecondInput"
                            Width="30" 
                            VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="7" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmSecondUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmSecondDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="8" Text="초에" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>
                            </StackPanel>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="메모 : " />
                            <TextBox
                        Name="AlarmMemo"
                        Grid.Column="1"
                        MinWidth="120" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="만들기" Click="StartAlarm_Click"></Button>
                            <Button Content="취소" Click="AlarmMakeCancel_Click"></Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
        <StackPanel Name="TimerMakePanel" VerticalAlignment="Bottom" Visibility="Hidden">
            <Border Background="Red" CornerRadius="10, 10, 0, 0">
                <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0, 30, 0, 30">
                    <StackPanel>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <TextBox Grid.Column="0" 
                                    Name="TimerHourInput"
                                    Width="30"
                                    VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="1" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerHourUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerHourDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="2" Text="시간" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                                <TextBox Grid.Column="3" 
                                    Name="TimerMinuteInput"
                                    Width="30" 
                                    VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="4" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerMinuteUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerMinuteDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="5" Text="분" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                                <TextBox Grid.Column="6" 
                                    Name="TimerSecondInput"
                                    Width="30" 
                                    VerticalContentAlignment="Center"/>

                                <Grid Grid.Column="7" Width="12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerSecondUpButton_Click" />
                                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerSecondDownButton_Click" />
                                </Grid>

                                <TextBlock Grid.Column="8" Text="초 후" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>
                            </StackPanel>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="메모 : " />
                            <TextBox
                                Name="TimerMemo"
                                Grid.Column="1"
                                MinWidth="120" />
                        </Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="만들기" Click="StartTimer_Click"></Button>
                            <Button Content="취소" Click="TimerMakeCancel_Click"></Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
    <!--
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="⏱️ 나의 타이머" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#333333"/>
        <Line Grid.Row="0" X1="0" X2="230" Y1="0" Y2="0" 
              Stroke="#CCCCCC" StrokeThickness="1" 
              StrokeDashArray="4 4" VerticalAlignment="Bottom" Margin="0,0,0,12"/>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5">
                <TextBox Grid.Column="0" 
                        Name="TimerHourInput"
                        Width="30"
                        Text="{Binding RemainingSeconds}" 
                        VerticalContentAlignment="Center"/>

                <Grid Grid.Column="1" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerHourUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerHourDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="2" Text="시간" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <TextBox Grid.Column="3" 
                        Name="TimerMinuteInput"
                        Width="30"
                        Text="{Binding RemainingSeconds}" 
                        VerticalContentAlignment="Center"/>

                <Grid Grid.Column="4" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerMinuteUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerMinuteDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="5" Text="분" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <TextBox Grid.Column="6" 
                        Name="TimerSecondInput"
                        Width="30"
                        Text="{Binding RemainingSeconds}" 
                        VerticalContentAlignment="Center"/>

                <Grid Grid.Column="7" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="TimerSecondUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="TimerSecondDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="8" Text="초 후" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <Button Content="타이머" Height="26" Click="StartTimer_Click" Background="#F5F5F5" BorderBrush="#CCCCCC" Cursor="Hand"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,10">
                <Button
                    Name="AmPmButton"
                     Content=""
                     Width="30"
                     Click="ToggleAmPm_Click"
                     Margin="0,0,4,0">
                </Button>
                <TextBox Grid.Column="0" 
                       Name="AlarmHourInput"
                       Width="30"
                       Text="{Binding RemainingSeconds}" 
                       VerticalContentAlignment="Center"/>

                <Grid Grid.Column="1" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmHourUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmHourDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="2" Text="시" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <TextBox Grid.Column="3" 
                       Name="AlarmMinuteInput"
                       Width="30"
                       Text="{Binding RemainingSeconds}" 
                       VerticalContentAlignment="Center"/>

                <Grid Grid.Column="4" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmMinuteUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmMinuteDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="5" Text="분" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <TextBox Grid.Column="6" 
                       Name="AlarmSecondInput"
                       Width="30"
                       Text="{Binding RemainingSeconds}" 
                       VerticalContentAlignment="Center"/>

                <Grid Grid.Column="7" Width="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="▲" FontSize="8" Padding="0" Click="AlarmSecondUpButton_Click" />
                    <Button Grid.Row="1" Content="▼" FontSize="8" Padding="0" Click="AlarmSecondDownButton_Click" />
                </Grid>

                <TextBlock Grid.Column="8" Text="초에" VerticalAlignment="Center" Margin="2,0,2,0" Foreground="#555555" FontSize="13"/>

                <Button Content="알람" Height="26" Click="StartAlarm_Click" Background="#F5F5F5" BorderBrush="#CCCCCC" Cursor="Hand"/>
            </StackPanel>
        </Grid>
        <Line Grid.Row="1" X1="0" X2="230" Y1="0" Y2="0" 
              Stroke="#CCCCCC" StrokeThickness="1" 
              StrokeDashArray="4 4" VerticalAlignment="Bottom" Margin="0,0,0,12"/>
        <ListView
            Grid.Row="2"
            x:Name="TimerListView"
            BorderBrush="Transparent"
            Background="Transparent"
            HorizontalContentAlignment="Stretch"
            Margin="0,5,0,0"
            
            ItemsSource="{Binding Timers}"
            d:DataContext="{d:DesignInstance Type=local:DesignViewModel, IsDesignTimeCreatable=True}">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                            Text="{Binding TimeLeftString}" 
                            FontSize="14"
                            FontWeight="Bold"
                            Margin="0,0,15,0"
                            VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="1"
                            Text="{Binding EndTimeString, StringFormat='종료: {0}'}" 
                            FontSize="12"
                            Foreground="Gray"
                            VerticalAlignment="Center"/>

                        <Button Grid.Column="2"
                            Content="{Binding PauseButtonText}"
                            Padding="5,2"
                            FontSize="11"
                            Margin="0,0,5,0"
                            Click="PauseTimer_Click"
                            Background="#FFF0F0"
                            BorderBrush="#FFE0E0"
                            VerticalAlignment="Center"/>

                        <Button Grid.Column="3"
                            Content="삭제"
                            Padding="5,2"
                            FontSize="11"
                            Click="DeleteTimer_Click"
                            Background="#FFF0F0"
                            BorderBrush="#FFE0E0"
                            VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        -->
</Window>
